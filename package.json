{
  "name": "gather-app",
  "scripts": {
    "build": "cd gatherappfrontend && npm install && npm run build && cd ..",
    "deploy": "cp -a gatherappfrontend/build/. public/",
    "postinstall": "npm run build && npm run deploy && echo 'Client built!'"
  }
}